# Session de travail — 2026-05-09 (exécution des pipelines)

**Objectif :** exécuter les scripts v2 du `PLAN_RECONSTRUCTION_PFE.md` et générer
les artefacts (modèles + résultats) pour les 4 modules ML.

---

## ✅ Tâches terminées dans cette session

### 1. CIC-IDS-2017 v2 — TERMINÉ
- **Preprocess** (`cicids2017/preprocessing/preprocess_v2.py`)
  - 500 000 lignes lues, `Destination Port` supprimé (anti-leakage), 51 features
  - Split stratifié 80/20 (pas de Timestamp dans le CSV → temporel impossible)
  - SMOTE sur train uniquement → 1 283 988 lignes équilibrées sur 4 classes
  - Artefacts : `cicids2017/data/processed_v2/{X,y}_{train,test}.csv`,
    `cicids2017/saved_models/{cicids_scaler.pkl, cicids_label_encoder.pkl, feature_columns.json}`
- **Training** (`cicids2017/models/train_xgboost_v2.py`)
  - Cross-val k=5 : **F1-macro = 0.9996 ± 0.0000**
  - Test final : **F1-macro = 0.9939, F1-weighted = 0.9997**
  - **Aucune feature dominante détectée** — top = `Fwd Packet Length Std` (0.232)
    → contraste fort avec v1 où Destination Port dominait. Anti-leakage validé.
  - Artefacts : `cicids2017/saved_models/xgb_model_v2.pkl`,
    `cicids2017/results_v2/{classification_report.txt, confusion_matrix.png,
    feature_importance.{csv,png}, roc_curves.png, pr_curves.png, metrics.json}`

### 2. ADFA-LD v2 — TERMINÉ
- **Bug fix dans `preprocess_adfa_v2.py`** : tous les fichiers normaux étaient
  rangés dans un seul groupe `"normal"` → GroupShuffleSplit envoyait tous les
  normaux soit en train soit en test. Correctif : 10 pseudo-groupes
  `normal_00` … `normal_09` (round-robin sur l'index).
- **Preprocess** : 833 normaux + 746 attaques, 60 familles d'attaque,
  X_train = (1340, 500), X_test = (239, 500), vectorizer fit-only train ✅
- **Training** : RF 200 arbres + CalibratedClassifierCV (isotonic, cv=5)
  - Cross-val StratifiedGroupKFold k=5 : **F1 = 0.9486 ± 0.0080**
  - Test : **F1 (seuil 0.5) = 0.9574, AUC = 0.9788**
  - Seuil calibré F1-optimal = 0.507
  - TPR par famille test : 13/14 familles d'attaque à 92-100 %, sauf
    `Adduser_9` à 63.6 % (faux négatifs sur petites séquences)
  - Normaux (`normal_07`, n=83) : TNR ≈ 96.4 % (FPR ≈ 3.6 %)
  - Artefacts : `adfa_ld/saved_models/{rf_adfa_model_v2.pkl, adfa_threshold.json, adfa_vectorizer.pkl, feature_columns.json}`,
    `adfa_ld/results_v2/{classification_report.txt, confusion_matrix.png,
    roc_curve.png, pr_curve.png, metrics.json}`

### 3. SIEM Windows — Données préparées, modèle EN COURS
- **Extraction APT29** : zips trouvés et extraits
  - `siem_windows/data/raw/day1/` : 385 MB JSON (196 081 events, 4 hosts)
  - `siem_windows/data/raw/day2/` : 1.7 GB JSON (587 286 events, 4 hosts)
- **Preprocess** : 132 fenêtres train (48 positives), 144 fenêtres test
  (**0 positives — bug à corriger**, voir ci-dessous), 36 features
- **Bug identifié** : les fenêtres d'attaque APT29 hardcodées pour day2
  (13:50–18:00 UTC) ne correspondent **pas** aux vrais timestamps day2
  (07:54–08:29 UTC). À corriger avant de re-lancer le preprocess.
  Day1 hardcodé (03:18–05:30 UTC) est en partie aligné (48/132 positives).

### 4. Infrastructure
- Configuration UTF-8 pour stdout (`PYTHONIOENCODING=utf-8`, `PYTHONUTF8=1`)
  pour éviter les `UnicodeEncodeError` sur Windows cp1252.
- Logs de chaque exécution sauvegardés dans `<module>/results_v2_*.log`.

---

## ⏳ Reste à faire pour finir la reconstruction

### Priorité 1 — Bloquant pour la démo

#### A. Corriger les fenêtres d'attaque APT29 day2 (1 h)
Dans `siem_windows/preprocessing/preprocess_siem.py`, remplacer la section
`APT29_ATTACK_WINDOWS["day2"]` par les vrais intervalles :
- Day1 file timeline : `2020-05-01T22:55Z` → ~`2020-05-02T07:54Z`
- Day2 file timeline : `2020-05-02T07:54Z` → `2020-05-02T08:29Z`
- Croiser avec l'emulation plan APT29 (`siem_dataset/.../apt29.xlsx`) pour
  retrouver les *vraies* timestamps de chaque technique exécutée.
- Alternative pragmatique : labelliser `1` toute la fenêtre du day1
  (les pen-testers tapent en continu) puis splitter day1 80/20 en
  temporel, garder day2 comme baseline non-supervisée.

Re-lancer ensuite :
```powershell
$env:PYTHONIOENCODING="utf-8"
.venv\Scripts\python.exe siem_windows/preprocessing/preprocess_siem.py
.venv\Scripts\python.exe siem_windows/training/train_siem.py
.venv\Scripts\python.exe siem_windows/evaluation/generate_siem_results.py
```

#### B. Lateral Movement (15-30 min)
Données Atomic Red Team déjà sur disque dans
`siem_dataset/data/otrf_datasets/datasets/atomic/windows/{lateral_movement,discovery,collection}/host/`.
Lancer :
```powershell
$env:PYTHONIOENCODING="utf-8"
.venv\Scripts\python.exe lateral_movement/preprocessing/preprocess_lateral.py
.venv\Scripts\python.exe lateral_movement/training/train_lateral.py
.venv\Scripts\python.exe lateral_movement/evaluation/generate_lateral_results.py
```

#### C. Vérification finale
```powershell
.venv\Scripts\python.exe scripts/check_artifacts.py
```
Doit afficher OK pour les 4 modèles.

### Priorité 2 — Démo end-to-end

1. **Tester `live_detection.py`** localement avec un Kafka mock ou réel.
2. **`docker-compose.yml`** : à créer pour démarrer Kafka + ES + Kibana + Logstash en 1 commande (le fichier annoncé par le plan n'existe pas encore).
3. **`integration/kibana/dashboard_soc.ndjson`** : à créer (manquant).
4. **Test producer** :
   ```powershell
   .venv\Scripts\python.exe scripts/produce_test_events.py --mode mixed
   ```
5. **Démo Atomic Red Team** sur PC Windows réel (T1003.001 lsass dump).

### Priorité 3 — Ingénierie

- Tests unitaires pytest sur `extract_siem_features`, `compute_composite_score`,
  `parse_event` (fonctions critiques de `live_detection.py`).
- Externaliser `THEHIVE_API_KEY` du Logstash conf via variable d'env.
- `.gitignore` propre (`.venv/`, `*.pkl > 50 MB`, `*.csv data`,
  `siem_windows/data/raw/`).
- Métriques Prometheus dans `live_detection.py`.
- Initialiser un repo git (le projet n'est pas encore versionné).

### Priorité 4 — Mémoire & soutenance

- Section « Avant / après remédiation » avec les chiffres :
  - CIC-IDS v1 : F1=1.00 (Destination Port = 0.5+ d'importance)
  - CIC-IDS v2 : F1=0.9939, top feature = Fwd Packet Length Std (0.232)
  - ADFA v1 : F1≈0.96 (vectorizer leak + split aléatoire)
  - ADFA v2 : F1=0.9574, AUC=0.9788, GroupShuffleSplit + vectorizer fit-only
- Schéma final d'architecture (mermaid).
- Tableau MITRE ATT&CK couvert par modèle.
- Argumentaire 3 minutes (déjà préparé dans `PLAN_RECONSTRUCTION_PFE.md` §7).

---

## 📊 Tableau de bord d'avancement

| Module | Preprocess | Training | Evaluation | Artefacts complets |
|---|---|---|---|---|
| CIC-IDS v2 | ✅ | ✅ | ✅ (intégré au train) | ✅ |
| ADFA-LD v2 | ✅ | ✅ | ✅ (intégré au train) | ✅ |
| SIEM Windows | ⚠️ (labels day2 = 0) | ❌ | ❌ | ❌ |
| Lateral Movement | ❌ | ❌ | ❌ | ❌ |
| live_detection.py | n/a | n/a | n/a | ❌ (dépend des 2 ci-dessus) |
| Kibana dashboard | n/a | n/a | n/a | ❌ |
| docker-compose | n/a | n/a | n/a | ❌ |

**Avancement global remédiation : ~50 %** (2/4 modèles terminés avec scores honnêtes).

---

## 🔑 Commandes utiles à mémoriser

```powershell
# Toujours définir l'encoding avant tout script Python du projet
$env:PYTHONIOENCODING="utf-8"
$env:PYTHONUTF8="1"

# Activer le venv si tu préfères
.\.venv\Scripts\Activate.ps1

# Vérifier les artefacts en 1 commande
.venv\Scripts\python.exe scripts/check_artifacts.py
```

---

*Document généré automatiquement le 2026-05-09 par session Claude Code.*
